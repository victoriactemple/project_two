<div> {{!-- containing div--}}
    <h1 class="title">up the garden path</h1>

    <div class="new-plant">
        <a href="/gardens/{{garden._id}}/plants/new" class="btn btn-small waves-effect orange lighten-3">Add a new Plant</a>
    </div>

    <div class="row">
        {{#each garden.plants}}

        <div class="col s12 m6 l3">
            <div class="card">
                <div class="card-image">
                    <a href="/gardens/{{../garden._id}}/plants/{{this._id}}"><img class="responsive-img" src="{{this.img}}"></a>
                </div>
            </div>
        </div>

        {{/each}}
    </div>
</div> {{!-- containing div--}} {{!-- {{#each garden.plants}}
<div class="row">
    <div class="col s12 m6 l3 plant-gallery">

        {{!--
        <div class="col s12 m6 l4"> --}}
            <div class="card">
                <div class="card-image waves-effect waves-block waves-light">
                    <a href="/gardens/{{../garden._id}}/plants/{{this._id}}"><img class="responsive-img" src="{{this.img}}"></a>
                </div>
            </div>
            {{!-- </div> --}}